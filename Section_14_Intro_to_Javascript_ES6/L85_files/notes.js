// Creating a variable and then outputting it in all-caps using .toUpperCase().
var myName = "Michael";
window.alert(myName.toUpperCase());

// Saving the variable in an UpperCase version.
myName = myName.toUpperCase();
myName

// Saving the variable in a LowerCase version:
myName = myName.toLowerCase();
myName
